# AGENTS.md

This repository uses **CLAUDE.md** as its single source of AI-assistant guidance.

If you are an AI agent other than Claude Code, read `CLAUDE.md` — it contains all the information you need:

- Build, test, and run commands (backend + frontend)
- Package-by-feature backend architecture and layer rules
- Frontend folder conventions and state management approach
- SOLID design principles applied to this codebase
- Security model (JWT, RBAC, SQL injection, XSS rules)
- Inter-module event contracts
- Database migration sequence and key schema decisions
- CI/CD pipeline and deployment setup
